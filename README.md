# Nemesis, your best friend!

## Introduction



## Setup

Install pnpm globally. Then setup is as simple as `pnpm install`.

Run `nemesis` with the command `pnpm dev`.

See `package.json` for more scripts!

Enjoy!
